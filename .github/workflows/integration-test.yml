name: Integration Test

on:
  pull_request:
    branches: [main]

permissions: {}

jobs:
  integration-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read

    services:
      mariadb:
        image: mariadb:11@sha256:09336a8c0cff9f363e133d8a245cca5ad3eeb326e0ffaedf74d49214c4571486
        env:
          MARIADB_ROOT_PASSWORD: test
          MARIADB_DATABASE: simplequest
        ports:
          - 3306:3306
        options: >-
          --health-cmd="healthcheck.sh --connect --innodb_initialized"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

      redis:
        image: redis:7-alpine@sha256:b1addbe72465a718643cff9e60a58e6df1841e29d6d7d60c9a85d8d72f08d1a7
        ports:
          - 6379:6379
        options: >-
          --health-cmd="redis-cli ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Java 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          java-version: "21"
          distribution: temurin
          cache: gradle

      - name: Setup Node.js (for bot scenarios)
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Setup pnpm (for bot dependencies)
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Install bot dependencies
        run: |
          cd bots
          pnpm install

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@3f131e8634966bd73d06cc69884922b02e6faf92 # v6.2.0
        with:
          cache-read-only: true

      - name: Build plugin
        run: ./gradlew shadowJar

      - name: Setup Paper server
        run: |
          mkdir -p artifacts

          # Download Paper 1.21.11 (latest stable)
          API="https://fill.papermc.io/v3/projects/paper"
          UA="SimpleQuest-CI/1.0"
          URL=$(curl -fsSL -H "User-Agent: $UA" \
            "$API/versions/1.21.11/builds" | \
            jq -r '[.[] | select(.channel == "STABLE")] | last
              | .downloads["server:default"].url')
          curl -fsSL -H "User-Agent: $UA" -o paper.jar "$URL"

          dir="servers/master"
          mkdir -p "$dir/plugins/SimpleQuest"

          printf 'eula=true\n' > "$dir/eula.txt"

          printf '%s\n' \
            "online-mode=false" \
            "server-port=25565" \
            "enable-rcon=true" \
            "rcon.port=25575" \
            "rcon.password=test" \
            > "$dir/server.properties"

          echo "=== build/libs contents ==="
          ls -la build/libs/
          echo "=== copying ==="
          cp -v build/libs/*.jar "$dir/plugins/" || echo "COPY FAILED"
          echo "=== plugins after copy ==="

          # Copy server config fixture (master: write-to-mysql=true)
          cp src/integrationTest/resources/master-config.yml \
            "$dir/plugins/SimpleQuest/config.yml"

          # Copy test quests (including bot quest)
          QDIR="$dir/plugins/SimpleQuest/@test/types"
          mkdir -p "$QDIR"
          cp src/integrationTest/resources/quests/test_quest.yml "$QDIR/"
          cp src/integrationTest/resources/quests/bot_quest.yml "$QDIR/"

          # Copy Paper jar
          cp paper.jar "$dir/"

      - name: Start server
        run: |
          dir="servers/master"

          cd "$dir"
          java -Xmx1G -jar paper.jar --nogui &
          echo $! > pid
          cd "$OLDPWD"

          # Wait for "Done" via tail -f on Paper's actual log file
          timeout 240 bash -c "
            until tail -f $dir/logs/latest.log 2>/dev/null | grep -q 'Done ('; do
              sleep 2
            done
          " || {
            echo 'Server failed to start! Last 50 lines:'
            tail -50 $dir/logs/latest.log
            exit 1
          }

          echo 'Server started on port 25565'

      - name: Run bot quest scenario
        run: |
          echo "=== Running bot quest integration scenario ==="
          cd bots
          # Wait a moment for server to stabilize, then run bot scenario
          sleep 5

          # Run the main GUI test (existing behavior)
          echo "--- Bot GUI test ---"
          npx tsx src/main.ts 2>&1 | tee ../artifacts/bot-output.log || {
            echo "Bot GUI test failed (may be expected if server still initializing)"
          }

          # Run the quest scenario (quest execution)
          echo "--- Bot quest scenario ---"
          npx tsx src/quest-scenario.ts 2>&1 | tee -a ../artifacts/bot-output.log || {
            echo "Bot quest scenario failed"
          }
          cd ..
          echo "=== Bot scenario complete ==="
          echo "=== latest.log (last 20 lines) ==="
          tail -20 servers/master/logs/latest.log || echo "NO LOG FILE"

      - name: Run kotest integration tests
        run: |
          echo "=== running kotest integration tests ==="
          ./gradlew integrationTestTask --no-daemon

      - name: Stop server
        if: always()
        run: |
          dir="servers/master"
          if [ -f "$dir/pid" ]; then
            kill $(cat "$dir/pid") 2>/dev/null || true
            echo "Server stopped"
          fi
          cp "$dir/logs/latest.log" artifacts/latest.log || true

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: integration-test-results
          path: |
            artifacts/*.log
            build/reports/tests/integrationTest/**
          retention-days: 7
